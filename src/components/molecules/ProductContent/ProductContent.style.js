import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  productContent: {
    marginTop: 8,
  },
  productLineText: {
    fontSize: 13,
    fontWeight: 'bold',
    marginBottom: 5,
  },
});
