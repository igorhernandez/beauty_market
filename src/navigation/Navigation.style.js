import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  backButton: {
    marginLeft: 10,
  },
  headerTitle: {
    fontWeight: 'bold',
    fontSize: 16,
  },
});

export {styles};
